[build]
builder = "RAILPACK"

[deploy]
startCommand = "python -m app.main serve-mcp --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
